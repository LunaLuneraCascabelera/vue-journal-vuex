
//Actions: son funciones (tareas) asincronas que pueden o no llamar a las mutaciones (van a llegar al backend - relacionado con el servidor y la base de datos)
// es decir son las que hacen impacto con la base de datos

// export const myAction = async({ commit }) => {
// }

export const loadEntries = async(/*{ commit }*/) => {

}

export const updateEntries = async(/*{ commit }*/) => {
    
}

export const createEntries = async(/*{ commit }*/) => {
    
}